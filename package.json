{
  "devDependencies": {
      "@babel/cli": "latest",
      "@babel/core": "latest",
      "@babel/plugin-proposal-class-properties": "latest",
      "@babel/plugin-proposal-decorators": "latest",
      "@babel/plugin-syntax-bigint": "latest",
      "dbus-next": "~0.10.2" 
  },
  "scripts": {
      "build": "rm -rf build && mkdir build && babel ./index.js -o ./build/index.js",
      "go": "node ./build",
      "start": "rm -rf build && mkdir build && babel ./index.js -o ./build/index.js && node ./build"
  }
}
